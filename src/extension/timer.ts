import {NodeCG} from '../../../../types/server';

export = (nodecg:NodeCG) => {
	const NanoTimer = require('nanotimer');
	const TimeObject = require('');

	const timer = new NanoTimer();
	const timerRep = nodecg.Replicant('timer');

	
};